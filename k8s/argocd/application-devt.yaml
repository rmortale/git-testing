apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: git-testing-devt
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    name: git-testing-devt
spec:
  project: default
  destination:
    name: in-cluster
    namespace: default
  sources:
    - chart: onechart
      repoURL: https://chart.onechart.dev
      targetRevision: 0.73.0
      helm:
        valueFiles:
          - $values/k8s/helm-values/k3d-devt-values.yaml

    - repoURL: https://github.com/rmortale/git-testing.git
      ref: values
      targetRevision: feature/add-service
  # syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true
  #   syncOptions:
  #     - CreateNamespace=true
  #     - ApplyOutOfSyncOnly=true
